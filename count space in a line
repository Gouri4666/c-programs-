#include<stdio.h>
#include<ctype.h>
#include<string.h>

int main()
{
	char s[25];
	int i,count=0;
	printf("Enter a string:");
 	 fgets(s, sizeof(s), stdin);
	for(i=0;s[i]!='\0';i++)
	{
	if(s[i]==' ')
	count++;
	}
	printf("Spaces=%d",count);
	return 0;
}
