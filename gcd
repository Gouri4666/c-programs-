#include<stdio.h>
#include<ctype.h>


int main()
{
	int a,b,i,gcd;
	printf("Enter 2 number:");
	scanf("%d%d",&a,&b);
	for(i=1;i<=10;i++)
	{
	if(a%i==0&&b%i==0)
	gcd=i;
	}
	printf("GCD=%d",gcd);
	return 0;
}
