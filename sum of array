#include<stdio.h>
#include<stdlib.h>
int main()
{
	int *a,i=0,n,sum=0;
	printf("Enter n:");
	scanf("%d",&n);
	a=(int *)malloc(n*sizeof(int));
	for(i=0;i<n;i++)
	{
	printf("\nEnter element %d:",(i+1));
	scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	sum+=a[i];
	printf("Sum =%d",sum);
	return 0;
}
