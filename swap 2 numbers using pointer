#include <stdio.h>
void swap(int *a,int *b )
{
	*a=*a+*b;
	*b=*a-*b;
	*a=*a-*b;
}
int main() 
{
	int x,y;
	int *a=&x,*b=&y;
	printf("Enter 2 number:\n");
	scanf("%d%d",a,b);
	printf("Before Swapping:%d %d\n",*a,*b);
	swap(&x,&y);
	printf("After swapping:");
	printf("%d %d",*a,*b);
	return 0;
}
