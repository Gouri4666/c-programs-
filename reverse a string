#include<stdio.h>
#include<ctype.h>
#include<string.h>

int main()
{
	char s[25];
	int i,len;
	printf("Enter a string:");
	scanf("%s",&s);
	len=strlen(s);
	for(i=len-1;i>=0;i--)
	{
	printf("%c",s[i]);
	}
	return 0;
}
