//LCM=n1*n2/GCD
#include<stdio.h>
#include<ctype.h>


int main()
{
	int a,b,i,gcd,lcm;
	printf("Enter 2 number:");
	scanf("%d%d",&a,&b);
	for(i=1;i<=10;i++)
	{
	if(a%i==0&&b%i==0)
	gcd=i;
	}
	printf("GCD=%d",gcd);
	lcm = (a * b) / gcd;
	printf("/nLCM=%d",lcm);

	return 0;
}
