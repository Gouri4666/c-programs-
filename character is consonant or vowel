#include<stdio.h>
#include<ctype.h>
int main()
{
	char ch,a;
	printf("Enter a character:");
	scanf("%c",&ch);
	a=tolower(ch);
	if(a=='a'||a=='e'||a=='i'||a=='o'||a=='u')
	printf("%c is a vowel",a);
	else
	printf("%c is a consonent",a);
	return 0;
}
